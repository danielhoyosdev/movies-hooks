import { makeStyles } from '@material-ui/styles';

export default makeStyles({
    container: {
        padding: 8,
    },
    imageContainer: {
        display: "flex",
        justifyContent: "center",
        marginBottom: 16,
    },
    image: {
        width: "100%",
        height: "100%",
    },
    plot: {
        marginBottom: 32,
    }, 
    subtitle: {
        marginRight: 8,
    }
});